# API Keys for Continuous Claude
# Copy this file to .env and fill in your actual keys
# The runtime uses these via ${VAR} placeholders in mcp_config.json

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# DESKTOP APP (no API key needed)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# RepoPrompt (for /rp-explorer, /onboard - token-efficient codebase exploration)
# Get from: https://repoprompt.com
# Install: App (Settings → Enable MCP Server) OR CLI (npm i -g @anthropics/repoprompt)
# Without: Falls back to bash (find, grep, etc.)

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL SERVICES (ALL have local fallbacks - see CLAUDE.md)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Braintrust (session tracing + learnings extraction)
# Get from: https://braintrust.dev
# LOCAL FALLBACK: claude-mem plugin (see below) - cross-session memory
BRAINTRUST_API_KEY=

# GitHub (for github-search skill)
# Get from: https://github.com/settings/tokens
# LOCAL FALLBACK: gh CLI (if installed)
GITHUB_PERSONAL_ACCESS_TOKEN=

# Perplexity (for perplexity-search skill)
# Get from: https://www.perplexity.ai/settings/api
# LOCAL FALLBACK: WebSearch builtin tool (no API key needed)
PERPLEXITY_API_KEY=

# Firecrawl (for firecrawl-scrape skill)
# Get from: https://www.firecrawl.dev/
# LOCAL FALLBACK: trafilatura - uv run python scripts/web_scrape_local.py <url>
FIRECRAWL_API_KEY=

# Morph (for morph-search skill)
# Get from: https://morphllm.com/
# LOCAL FALLBACK: Grep tool (ripgrep) - builtin, no API key needed
MORPH_API_KEY=

# Nia (for nia-docs skill)
# Get from: https://trynia.ai/
# LOCAL FALLBACK: Context7 MCP - mcp__context7__get-library-docs
NIA_API_KEY=

# Supabase (for supabase MCP server)
# Get from: https://supabase.com/dashboard/project/_/settings/api (Project Settings → API → Service Role Key)
# Without: No Supabase database/functions access

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LOCAL PLUGINS (No API Key Required)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# claude-mem (cross-session memory via plugin)
# Install: /plugin marketplace add thedotmack/claude-mem && /plugin install claude-mem
# Data: ~/.claude-mem/ (SQLite + Chroma)
# Worker: http://localhost:37777
# Without: No automatic context injection or memory search
CLAUDE_MEM_WORKER_PORT=37777
